import React from "react";
import { IonContent, IonPage, IonText } from '@ionic/react';

const LogoutPage: React.FC = () => (
  <IonPage>
    <IonContent className="ion-padding">
      <IonText>LogoutPage</IonText>
    </IonContent>
  </IonPage>
);

export default LogoutPage;
