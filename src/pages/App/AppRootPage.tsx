import React from "react";
import { IonContent, IonPage, IonText } from '@ionic/react';

const AppRootPage: React.FC = () => (
  <IonPage>
    <IonContent className="ion-padding">
      <IonText>AppRootPage</IonText>
    </IonContent>
  </IonPage>
);

export default AppRootPage;
